{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Age of Sails",
            "type": "lldb",
            "request": "launch",
            "terminal": "external",
            "program": "${workspaceFolder}/output.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
        }
    ]
}